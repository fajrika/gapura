# Lokal (non-Docker): DB jalan sendiri (misal: docker run -d --name rtrw-db \
#   -e POSTGRES_USER=rtrw -e POSTGRES_PASSWORD=rtrw_secret -e POSTGRES_DB=rtrw \
#   -p 5433:5432 -v rtrw-pgdata:/var/lib/postgresql postgres:18-alpine)
DATABASE_URL="postgres://rtrw:rtrw_secret@localhost:5433/rtrw"

# Secret untuk JWT session (wajib diganti dengan string acak saat produksi)
JWT_SECRET="ganti-dengan-secret-acak"

# Kunci VAPID untuk push notification (generate: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
NEXT_PUBLIC_VAPID_KEY=""

# URL publik aplikasi (dipakai notifikasi push & tautan)
NEXT_PUBLIC_APP_URL="http://localhost:3003"

# --- Variabel untuk docker-compose (dapat disalin ke .env di root repo) ---
# POSTGRES_USER=rtrw
# POSTGRES_PASSWORD=rtrw_secret
# POSTGRES_DB=rtrw
# Port app di host untuk compose: 3003 (bisa diubah)
